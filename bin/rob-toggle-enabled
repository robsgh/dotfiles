#!/bin/bash

# Ensure sibling rob-* helpers resolve when launched by Hyprland without shell PATH updates.
script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
export PATH="$script_dir:$PATH"

# Desktop:summary=Check if a toggle is enabled (flag file exists)
# Desktop:args=<flag-name>

[[ -f "$HOME/.local/state/dotfiles/toggles/$1" ]]
